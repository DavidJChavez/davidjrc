---
interface Props {
	delay?: number;
	as?: keyof HTMLElementTagNameMap;
	class?: string;
	[key: string]: unknown;
}

const { delay = 0, as: Tag = 'div', class: className = '', ...rest } = Astro.props;
---

<Tag class:list={['reveal', className]} data-delay={delay} {...rest}>
	<slot />
</Tag>
